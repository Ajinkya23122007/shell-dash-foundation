# Core App Foundation

Build only Step 1 of the build order: app shell, routing, sidebar/nav, and the dashboard with mock stat cards. Don't build the map or other pages yet."

## Build 
- **Ship faster**: describe what you want to build and handles the code.
- **Stay in sync**: every change made is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally  [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
